import PlaceholderPage from "./PlaceholderPage";

export default function HRTeam() {
  return (
    <PlaceholderPage
      title="HR Team"
      description="Team management tools are still being finalized. In the meantime, you can access user controls and permissions from the admin panel."
      actionLabel="Open Settings"
      actionPath="/admin/settings"
    />
  );
}
